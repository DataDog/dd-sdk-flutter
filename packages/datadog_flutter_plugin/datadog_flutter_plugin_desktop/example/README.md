# datadog_flutter_plugin_desktop example

This example is mostly a stub for generating FFI bindings.

For an example of how to use Datadog in your application, refer
to the example in the root `datadog_flutter_plugin` package.