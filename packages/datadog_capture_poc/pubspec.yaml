name: datadog_capture_poc
description: A widget for capturing Flutter trees
version: 1.0.0-alpha.1
homepage:

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  collection: ^1.15.0
  flutter:
    sdk: flutter
  http: ^0.13.4
  json_annotation: ^4.5.0
  uuid: ^3.0.6
  image: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  json_serializable: ^6.2.0
  build_runner: ^2.1.10

flutter:
